spring:
  application:
    name: data-processing-platform-batch
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  jackson:
    deserialization:
      fail-on-unknown-properties: false

server:
  port: 8081
